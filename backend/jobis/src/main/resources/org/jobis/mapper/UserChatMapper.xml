<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.jobis.mapper.UserChatMapper">

	<!-- 유저 채팅 input-->
	<insert id="register">
		INSERT INTO tbl_userchat (
			cno,
			r_title,
			r_tag,
			leader,
			r_regdate,
			sch_date,
			enabled
		)
		VALUES (
			seq_cno.nextval,
			#{r_title},
			#{r_tag},
			#{leader},
			SYSDATE,
			TO_DATE(#{sch_date}, 'YYYY-MM-DD HH24:MI:SS'),
			1
		)
	</insert>
	
	<!-- 유저 채팅 불러오기 -->
	<select id="getUserChat" resultType="org.jobis.domain.CJSVO">
	  SELECT uc.*, u.name AS leader_name
	  FROM tbl_userchat uc
	  LEFT JOIN tbl_user u ON uc.leader = u.uno
	  ORDER BY uc.cno DESC
		<!-- SELECT * FROM tbl_userchat ORDER BY cno DESC -->
	</select>
	
	<!-- 이름 가져오기 -->
	<select id="getNameByUno" resultType="org.jobis.domain.UserVO">
		SELECT * FROM tbl_user where uno=#{uno}
	</select>
	
	<!-- 채팅 참여하기 -->
	<update id="joinChat">
		UPDATE tbl_userchat
		SET member = #{member}
		WHERE cno = #{cno}
	</update>

</mapper>
